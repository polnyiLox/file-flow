[project]
name = "start-app-mongo"
version = "0.1.0"
description = "Reusable FastAPI application template with MongoDB"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pydantic-settings>=2.15.0",
    "pymongo>=4.17.0",
    "uvicorn>=0.52.4",
]
